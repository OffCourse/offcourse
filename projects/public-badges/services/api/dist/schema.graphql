schema {
  query: Query
}

type PublicBadge {
  badgeId: ID
  badgeClassId: ID
  name: String!
  tags: [String]
  description: String
  narrative: String
  recipientId: ID
  issuedOn: String
  expires: String
}

type Query {
  getBadge(badgeId: ID): PublicBadge
  getAllBadges: [PublicBadge]
}


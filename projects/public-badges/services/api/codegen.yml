overwrite: true
schema: src/schema/**/*.graphql
config:
  useIndexSignature: true
documents: null
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      avoidOptionals: true
      contextType: ../types#ApolloContext
      mappers:
        PublicBadge: ../types#OpenBadge
        RequestedPublicBadge: ../types#OpenBadge
        ApprovedPublicBadge: ../types#OpenBadge
        SignedPublicBadge: ../types#OpenBadge
        Proof: ../types#OpenBadgeProof
        OpenBadgeArtifact: ../types#OpenBadge
  ./graphql.schema.json:
    plugins:
      - "introspection"

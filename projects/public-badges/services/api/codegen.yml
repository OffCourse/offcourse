overwrite: true
schema: src/schema/**/*.graphql
config:
  useIndexSignature: true
documents: null
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      skipTypename: true
      avoidOptionals: true
      contextType: ../types#ApolloContext
      scalars:
        EmailAddress: string
        JSON: string
        UUID: string
      mappers:
        ValueCase: ../types#ValueCaseProxy
        PublicBadge: ../types#PublicBadgeProxy
  ./graphql.schema.json:
    plugins:
      - "introspection"

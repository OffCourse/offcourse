type Query {
  getBadge(badgeId: ID!): PublicBadge
  getAllBadges(domainName: URL): [PublicBadge]
  getOrganization(organizationId: ID, domainName: URL): Organization
  getAllOrganizations(filter: OrganizationStatus): [Organization]
  getValueCase(valueCaseId: ID!): ValueCase
  getAllValueCases: [ValueCase]
}

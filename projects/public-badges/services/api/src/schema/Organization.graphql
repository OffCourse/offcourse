type Contact {
  name: String
  email: String
}

type Organization {
  organizationId: ID!
  status: Status!
  path: String!
  name: String!
  contact: Contact!
  admin: Contact!
  domains: [String]!
}

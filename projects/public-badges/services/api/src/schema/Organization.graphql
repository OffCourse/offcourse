type Contact {
  name: String
  email: String
}

type Domains {
  main: String!
  other: [String]
}

type Organization {
  organizationId: ID!
  status: Status!
  path: String!
  name: String!
  contact: Contact!
  admin: Contact!
  domains: Domains!
}

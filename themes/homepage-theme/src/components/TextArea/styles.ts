import { wrapperStyles as inputWrapperStyles, inputStyles } from "../Input/styles";

export const wrapperStyles = { ...inputWrapperStyles };

export const textAreaStyles = {
  ...inputStyles,
  fontFamily: "body",
  fontSize: 1,
  lineHeight: 1
}
